export declare enum PomodoroMode {
    WORK = "work",
    SHORT_BREAK = "shortBreak",
    LONG_BREAK = "longBreak"
}
export declare enum PomodoroStatus {
    IDLE = "idle",
    RUNNING = "running",
    PAUSED = "paused",
    COMPLETED = "completed"
}
